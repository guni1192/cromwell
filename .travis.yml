language: rust
rust:
  - nightly
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true
before_script:
  - rustup component add rustfmt-preview
script:
  - cargo build --verbose --all
  - cargo fmt --all -- --check
